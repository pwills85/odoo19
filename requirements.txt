# Production Requirements - l10n_cl_dte & l10n_cl_dte_enhanced
# Week 2: Frontend Development - PDF Reports & Dashboard

# ============================================================================
# PDF417 Barcode Generation (Week 2)
# ============================================================================
# Required for TED (Timbre Electrónico) generation in DTE reports
pdf417==1.1.0           # PDF417 2D barcode generation (SII requirement)
Pillow>=11.0.0          # Image processing for barcode rendering (CVE-2023-44271, CVE-2023-50447, CVE-2024-28219 fixed)

# ============================================================================
# XML Processing (Already in Odoo, but explicit for clarity)
# ============================================================================
lxml>=5.3.0             # XML generation and parsing (CVE-2024-45590 fixed, major upgrade 4.x→5.x)
xmlsec>=1.3.13          # XML digital signatures (DTE signing)

# ============================================================================
# SOAP Client (SII Communication)
# ============================================================================
zeep>=4.2.1             # SOAP client for SII webservices
requests>=2.32.3        # HTTP client (CVE-2023-32681 fixed)

# ============================================================================
# Cryptography (Certificate handling)
# ============================================================================
cryptography>=46.0.3    # Digital certificates and encryption (CVE-2023-50782, CVE-2024-0727 fixed)
pyOpenSSL>=24.2.1       # SSL/TLS certificate handling (CVE-2023-0286 fixed)

# ============================================================================
# Utilities
# ============================================================================
python-dateutil>=2.8.2  # Date/time utilities
pytz                    # Timezone handling (Chilean timezone)
python-stdnum>=1.19     # P3.2 GAP CLOSURE: RUT validation (Chilean tax ID)

# ============================================================================
# Redis (Sprint 0.2/0.3 - Enterprise Security)
# ============================================================================
redis>=5.0.0            # Rate limiting, replay protection, caching

# ============================================================================
# Optional: Excel Export (Dashboard feature)
# ============================================================================
openpyxl>=3.1.2         # Excel export for dashboard reports

# ============================================================================
# Optional: QR Code (Fallback if PDF417 fails)
# ============================================================================
qrcode>=7.4.2           # QR code generation (fallback)

# ============================================================================
# Notes:
# - lxml, requests, python-dateutil, pytz are already in Odoo core
# - We explicitly list them for documentation and version pinning
# - pdf417 and Pillow are NEW dependencies for Week 2
# - openpyxl for Excel export in dashboard (optional, Week 3)
# ============================================================================
